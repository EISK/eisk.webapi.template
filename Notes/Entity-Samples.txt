========
Samples Use Cases (platform agonistic)

* Empty (no domain)

Single Entity

* Minimal code domain crud - Web controller base, with generic domain and data service

* All base classes (no custom method)
* All base classes (with custom crud methods)

* No base classes (with custom method and crud)
* No base classes (create and get only?) - using service utility class as property instead of using as base class

Multiple Entities
* 1:m
* m:1
* m:m

Query Features
* Raw text query
* Aggregate query (count, sum)
* Stored procedure
* Db function
* View
========

